# DataClaw Python Package

This package installs the DataClaw FastAPI backend, bundled UI assets, and the `dataclaw` CLI.

After installation:

```bash
dataclaw init
dataclaw start
```

Source repo and Docker Compose path: <https://github.com/saivangapally81/dataclaw>
